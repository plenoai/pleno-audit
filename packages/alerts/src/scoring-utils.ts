/**
 * @fileoverview Scoring Utilities — Re-export from @libztbs/types
 *
 * スコアリングユーティリティは @libztbs/types に移動しました。
 * 後方互換のため re-export を維持しています。
 */
export {
  scoreToRiskLevel5,
  scoreToExtensionRiskLevel,
  getStatusBadge,
  RISK_SCORE_THRESHOLDS,
  type RiskLevel5,
  type ExtensionRiskLevel,
  type StatusBadge,
  type StatusBadgeVariant,
} from "@libztbs/types";
